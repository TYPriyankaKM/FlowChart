import { Node } from '../types';
interface UseVisibleNodes {
    visibleNodes: Node[];
    nodes: Node[];
}
declare const useVisibleNodes: ({ onlyRenderVisibleNodes }: {
    onlyRenderVisibleNodes?: boolean | undefined;
}) => UseVisibleNodes;
export default useVisibleNodes;
